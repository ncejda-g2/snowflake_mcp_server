name: CI

on:
  pull_request:
    types:
      - synchronize
      - opened

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  test:
    uses: ./.github/workflows/test.yml
    secrets: inherit
    permissions:
      contents: read
      pull-requests: write
      issues: write
